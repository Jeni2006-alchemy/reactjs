function WorkshopForm() {
  const [data, setData] = React.useState("");

  return (
    <div>
      <input onChange={e => setData(e.target.value)} placeholder="Your Name" />
      <p>👀 Preview: {data}</p>
    </div>
  );
}
